apiVersion: quay.redhat.com/v1
kind: QuayRegistry
metadata:
  name: image-registry
  namespace: ${NAMESPACE}
spec:
  configBundleSecret: config-bundle-secret
  components:
    - kind: objectstorage
      managed: false
